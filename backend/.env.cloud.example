# ─── Ventro Cloud Environment Variables ───────────────────────────────────────
# Copy this to .env on your cloud backend (Render / Railway etc.)
# ALL values below MUST be set before deploying.

# ── Application ────────────────────────────────────────────────────────────────
APP_ENV=production
APP_NAME=Ventro
APP_VERSION=1.0.0
DEBUG=false
APP_SECRET_KEY=REPLACE_WITH_openssl_rand_-hex_32

# CORS: your Vercel frontend URL
ALLOWED_ORIGINS=["https://ventro.vercel.app"]

# ── LLM: Groq (free tier) ─────────────────────────────────────────────────────
# Get key at: https://console.groq.com → API Keys
GROQ_API_KEY=gsk_REPLACE_WITH_YOUR_KEY
GROQ_MODEL=llama-3.3-70b-versatile
# Leave OLLAMA vars blank — Groq takes priority when GROQ_API_KEY is set
OLLAMA_BASE_URL=
OLLAMA_PRIMARY_MODEL=

# ── PostgreSQL: Supabase (free) ────────────────────────────────────────────────
# Get from: https://supabase.com → Project Settings → Database → Connection string (Transaction mode)
# Replace each <FIELD> with your actual values from Supabase dashboard:
DATABASE_URL=postgresql+asyncpg://<SUPABASE_USER>:<SUPABASE_PASSWORD>@<SUPABASE_HOST>:6543/postgres

# Get from: https://cloud.mongodb.com → Connect → Connect your application → copy URI
# Replace each <FIELD> with your actual values from Atlas dashboard:
MONGO_URL=scheme+srv://<ATLAS_USER>:<ATLAS_PASSWORD>@<ATLAS_HOST>/ventro_docs?retryWrites=true&w=majority
# Note: replace 'scheme' above with 'mongodb' when pasting your real URI
MONGO_DB=ventro_docs

# ── Redis: Upstash (free) ─────────────────────────────────────────────────────
# Get from: https://console.upstash.com → Redis → Connect → .env
REDIS_URL=rediss://default:PASSWORD@REGION.upstash.io:6379

# ── Qdrant: Qdrant Cloud (free 1 GB cluster) ──────────────────────────────────
# Get from: https://cloud.qdrant.io → Cluster → API Keys
QDRANT_HOST=XXXX.us-east4-0.gcp.cloud.qdrant.io
QDRANT_PORT=6333
QDRANT_API_KEY=REPLACE_WITH_QDRANT_API_KEY
QDRANT_COLLECTION_NAME=ventro_docs

# ── Embeddings ─────────────────────────────────────────────────────────────────
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_DIMENSION=384

# ── Document Processing ────────────────────────────────────────────────────────
MAX_UPLOAD_SIZE_MB=50
TEMP_UPLOAD_DIR=/tmp/ventro_uploads

# ── SAMR ───────────────────────────────────────────────────────────────────────
SAMR_ENABLED=true
SAMR_DIVERGENCE_THRESHOLD=0.85
SAMR_PERTURBATION_STRENGTH=0.1

# ── JWT (for future auth) ──────────────────────────────────────────────────────
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
